const https = require('https'); https.get('https://comis-io-kelompok-5-frontend.vercel.app', (res) => { let body = ''; res.on('data', d => body += d); res.on('end', () => { const match = body.match(/src="(\/assets\/index-[^"]+\.js)"/); if (!match) return console.log('no match'); https.get('https://comis-io-kelompok-5-frontend.vercel.app' + match[1], (r) => { let b = ''; r.on('data', d => b+=d); r.on('end', () => console.log('Contains Tag?', b.includes('Tag'), 'Contains LinkIcon?', b.includes('LinkIcon'))) }) }) })
